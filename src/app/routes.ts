import { Routes } from '@angular/router';
import { WeathercomComponent } from './weathercom/weathercom.component';

export const allAppRoutes: Routes = [
  { path: '', component: WeathercomComponent }
];